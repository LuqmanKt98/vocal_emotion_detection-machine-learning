{
  "name": "emotion-detector-backend",
  "version": "1.0.0",
  "description": "Backend for Vocal Emotion Detection app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-python-deps": "pip install -r requirements.txt",
    "setup": "npm install && npm run install-python-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "python-shell": "^5.0.0",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 